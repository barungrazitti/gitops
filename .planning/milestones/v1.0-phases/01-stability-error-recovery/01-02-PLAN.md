---
phase: 01-stability-error-recovery
plan: 02
type: execute
wave: 2
depends_on: [01]
files_modified: [src/index.js]
autonomous: true
requirements: [STAB-02]

must_haves:
  truths:
    - "AICommitGenerator.getAISuggestion is implemented and uses configured AI provider"
    - "AI-powered suggestions are 1-sentence solutions for common errors"
    - "AI suggestions are attempted before local fallback"
  artifacts:
    - path: "src/index.js"
      provides: "AI integration in error suggestions"
  key_links:
    - from: "src/index.js (provideErrorSuggestions)"
      to: "this.getAISuggestion"
      via: "primary suggestion path"
      pattern: "this\\.getAISuggestion\\(error, .*\\)"
---

<objective>
Enhance the error suggestion system with AI-powered troubleshooting, providing more intelligent and context-aware solutions to the user.

Purpose: Implement STAB-02 requirements using research findings.
Output: Dynamic, AI-powered error suggestions.
</objective>

<execution_context>
@/Users/barun.tayenjam/.gemini/get-shit-done/workflows/execute-plan.md
@/Users/barun.tayenjam/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@01-RESEARCH.md
@src/index.js
@src/providers/base-provider.js
</context>

<tasks>

<task type="auto">
  <name>Task 1: Implement AI Suggestion Helpers</name>
  <files>src/index.js</files>
  <action>
    Implement two new helper methods in `AICommitGenerator`:
    1. `isAIAvailable(options)`: Checks if a provider is configured and available (e.g., checks `this.configManager`).
    2. `getAISuggestion(error, options)`: 
       - Builds a specific prompt for the AI to provide a solution for the error. Use the prompt pattern from 01-RESEARCH.md.
       - Identifies the current provider (Groq or Ollama).
       - Calls the provider's `generateResponse` method (which both providers already implement).
       - Returns a 1-sentence solution or null if anything fails.
  </action>
  <verify>
    <automated>npm test tests/error-handling.test.js</automated>
  </verify>
  <done>AI integration helpers are ready to be used.</done>
</task>

<task type="auto">
  <name>Task 2: Integrate AI into provideErrorSuggestions</name>
  <files>src/index.js</files>
  <action>
    Update `provideErrorSuggestions(error, options)` to try AI first:
    1. If `isAIAvailable(options)` is true, try calling `getAISuggestion(error, options)`.
    2. If a suggestion is returned, display it and RETURN early.
    3. If no AI suggestion is available (or an error occurred during generation), proceed to display the local suggestion from Plan 01.
    
    Ensure `ora` spinner is correctly managed (stopped before suggestions are shown).
  </action>
  <verify>
    <automated>npm test tests/error-handling.test.js</automated>
  </verify>
  <done>The error recovery system prioritizes AI-powered suggestions but falls back gracefully.</done>
</task>

</tasks>

<verification>
Manual verification using a valid but configured AI provider to trigger an error (e.g., passing invalid diff). Verify AI suggestion is shown.
</verification>

<success_criteria>
- AICommitGenerator has isAIAvailable and getAISuggestion.
- AI-powered suggestions are shown when possible.
- Local suggestions are shown when AI fails.
</success_criteria>

<output>
After completion, create `.planning/phases/01-stability-error-recovery/01-02-SUMMARY.md`
</output>
