---
wave: 4
depends_on:
  - 03-01-PLAN.md
  - 03-02-PLAN.md
  - 03-03-PLAN.md
files_modified:
  - src/utils/metrics-scorer.js
  - src/core/message-formatter.js
  - bin/aicommits
  - tests/metrics-scorer.test.js
requirements_addressed: []
autonomous: true
---

## Objective

Implement automated commit message quality metrics scoring with specificity, conventional format, and length checks, displayed by default with an optional --quiet flag.

## Tasks

<task>
  <read_first>
    - .planning/phases/03-improve-the-relevance-of-the-git-commit-messages-for-small-and-large-diffs/03-CONTEXT.md
  </read_first>
  <action>
    Create src/utils/metrics-scorer.js module:
    - Implement calculateSpecificityScore(message, diff) function:
      * Extract keywords from message (lowercase, remove conventional prefix)
      * Extract entities from diff using EntityExtractor
      * Calculate overlap percentage: (matching entities / total entities) * 100
      * Return score 0-100
    - Implement checkConventionalFormat(message) function:
      * Check for type(scope): description pattern
      * Validate type against allowed list: feat, fix, docs, style, refactor, perf, test, chore, ci, build
      * Return { isConventional: boolean, issues: string[] }
    - Implement checkLengthCompliance(message) function:
      * Check first line length <= 72 characters
      * Check first line does not end with period
      * Check imperative mood (starts with capital letter, no "ing" words)
      * Return { isCompliant: boolean, issues: string[] }
    - Export: calculateSpecificityScore, checkConventionalFormat, checkLengthCompliance
  </action>
  <acceptance_criteria>
    - src/utils/metrics-scorer.js exists
    - calculateSpecificityScore returns number 0-100
    - checkConventionalFormat returns object with isConventional boolean
    - checkLengthCompliance returns object with isCompliant boolean
    - grep -q "overlap percentage" src/utils/metrics-scorer.js
  </acceptance_criteria>
</task>

<task>
  <read_first>
    - src/utils/metrics-scorer.js
    - tests/metrics-scorer.test.js
  </read_first>
  <action>
    Create tests/metrics-scorer.test.js:
    - Test calculateSpecificityScore with entity overlap: message "fix(auth): resolve validateToken error" with diff containing validateToken returns high score
    - Test calculateSpecificityScore with no overlap: generic message returns low score
    - Test checkConventionalFormat with valid message: "feat(api): add endpoint" returns { isConventional: true, issues: [] }
    - Test checkConventionalFormat with invalid type: "invalid(scope): description" returns { isConventional: false, issues: ["Invalid type"] }
    - Test checkLengthCompliance with compliant message: <= 72 chars, no period, imperative mood returns { isCompliant: true, issues: [] }
    - Test checkLengthCompliance with non-compliant: > 72 chars or ends with period returns { isCompliant: false, issues: [...] }
    - Use Jest describe/test blocks
  </action>
  <acceptance_criteria>
    - tests/metrics-scorer.test.js exists
    - All tests pass when running npm test
    - Tests cover all three scoring functions
    - grep -q "describe('MetricsScorer'" tests/metrics-scorer.test.js
  </acceptance_criteria>
</task>

<task>
  <read_first>
    - src/utils/metrics-scorer.js
  </read_first>
  <action>
    Implement score categorization in metrics-scorer.js:
    - Create categorizeScore(score) function:
      * 0-60: Poor
      * 61-80: Fair
      * 81-90: Good
      * 91-100: Excellent
    - Return { category: string, color: string } where color is ANSI code for terminal display
    - Poor: red (31), Fair: yellow (33), Good: green (32), Excellent: bright green (92)
    - Export categorizeScore function
  </action>
  <acceptance_criteria>
    - metrics-scorer.js contains categorizeScore function
    - Returns category string (Poor/Fair/Good/Excellent)
    - Returns ANSI color code
    - Score thresholds: 0-60 Poor, 61-80 Fair, 81-90 Good, 91-100 Excellent
    - grep -q "91-100" src/utils/metrics-scorer.js
  </acceptance_criteria>
</task>

<task>
  <read_first>
    - src/utils/metrics-scorer.js
  </read_first>
  <action>
    Implement overall quality scoring in metrics-scorer.js:
    - Create calculateOverallScore(message, diff) function:
      * Get specificity score: calculateSpecificityScore(message, diff)
      * Get conventional check: checkConventionalFormat(message)
      * Get length check: checkLengthCompliance(message)
      * Calculate weighted average: specificity (50%), conventional (30%), length (20%)
      * Return { overall: number, specificity: number, conventional: boolean, length: boolean, issues: string[] }
    - Export calculateOverallScore function
  </action>
  <acceptance_criteria>
    - metrics-scorer.js contains calculateOverallScore function
    - Returns object with overall number score
    - Returns breakdown: specificity, conventional, length, issues
    - Weighted average: 50% specificity, 30% conventional, 20% length
    - grep -q "50%" src/utils/metrics-scorer.js
  </acceptance_criteria>
</task>

<task>
  <read_first>
    - src/core/message-formatter.js
    - src/utils/metrics-scorer.js
  </read_first>
  <action>
    Integrate quality metrics into MessageFormatter:
    - Add require: const MetricsScorer = require('./metrics-scorer')
    - In constructor: this.metricsScorer = new MetricsScorer()
    - Create formatWithMetrics(message, diff) method:
      * Calculate overall score using calculateOverallScore
      * Get category using categorizeScore
      * Format metrics display: "Quality: 85/100 (Good) - Specificity: ✓ Conventional: ✓ Length: ✓"
      * Return formatted string with metrics appended
    - Store metrics in result object for programmatic access
  </action>
  <acceptance_criteria>
    - src/core/message-formatter.js contains "require('./metrics-scorer')"
    - formatWithMetrics method exists
    - Returns formatted string with quality metrics
    - Output format: "Quality: XX/100 (Category) - Specificity: X Conventional: X Length: X"
    - grep -q "Quality:" src/core/message-formatter.js
  </acceptance_criteria>
</task>

<task>
  <read_first>
    - bin/aicommits
  </read_first>
  <action>
    Add --evaluate flag to CLI:
    - Add --evaluate or -e option to commander options
    - Add --quiet or -q option to suppress metrics display
    - Store options in config: options.evaluate, options.quiet
    - Pass options to message formatter
    - When evaluate flag is set, always show metrics (unless --quiet)
    - Default behavior: show metrics by default, use --quiet to suppress
  </action>
  <acceptance_criteria>
    - bin/aicommits contains --evaluate option
    - bin/aicommits contains --quiet option
    - Options passed to message formatter
    - Metrics shown by default
    - grep -q "--evaluate" bin/aicommits
  </acceptance_criteria>
</task>

<task>
  <read_first>
    - bin/aicommits
    - src/core/message-formatter.js
  </read_first>
  <action>
    Display metrics in CLI output:
    - After commit message generation, call formatWithMetrics
    - Display metrics using console.log with color codes
    - Format: "✨ Quality: 85/100 (Good)"
    - Show breakdown with checkmarks/crosses: "  - Specificity: ✓ (82/100)"
    - Show issues if any: "  - Issues: First line exceeds 72 characters"
    - Respect --quiet flag to suppress display
  </action>
  <acceptance_criteria>
    - Metrics displayed in CLI after commit message
    - Uses color codes (green for good, red for poor)
    - Shows checkmark ✓ or cross ✗ for each metric
    - Shows detailed issues when present
    - --quiet flag suppresses output
    - grep -q "✨ Quality:" bin/aicommits
  </acceptance_criteria>
</task>

<task>
  <read_first>
    - src/core/message-formatter.js
  </read_first>
  <action>
    Add metrics logging to activity logger:
    - Import activityLogger in message-formatter.js
    - After calculating metrics, log to activity logger:
      * Log overall score
      * Log specificity score
      * Log conventional compliance
      * Log length compliance
      * Log category (Poor/Fair/Good/Excellent)
    - Enable analysis of quality trends over time
    - Use existing activity logging infrastructure
  </action>
  <acceptance_criteria>
    - message-formatter.js imports activityLogger
    - Logs metrics after calculation
    - Logs include overall score and category
    - grep -q "activityLogger.*metrics" src/core/message-formatter.js
  </acceptance_criteria>
</task>

## Verification Criteria

1. Run `npm test` - all tests pass including new metrics-scorer tests
2. Test specificity scoring: message with entity overlap scores higher than generic message
3. Test conventional format: valid conventional message passes check, invalid fails with issues
4. Test length compliance: 50-char first line passes, 80-char fails with issue
5. Test overall scoring: weighted average calculated correctly
6. Test categorization: score 85 returns "Good" with green color
7. Test CLI: run `aicommits --evaluate` shows quality metrics
8. Test quiet mode: run `aicommits --quiet` suppresses metrics display
9. Verify default behavior: metrics shown without --evaluate flag

## Must-Haves

- Three core metrics: specificity score, conventional format, length compliance
- Specificity measures entity overlap between message and diff
- Conventional checks type(scope): description pattern
- Length checks 72 char limit, no period, imperative mood
- Score categories: 0-60 Poor, 61-80 Fair, 81-90 Good, 91-100 Excellent
- Metrics shown by default, --quiet to suppress
- Color-coded display with checkmarks/crosses
