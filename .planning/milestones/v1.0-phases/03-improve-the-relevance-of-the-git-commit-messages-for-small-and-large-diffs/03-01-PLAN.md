---
wave: 1
depends_on: []
files_modified:
  - package.json
  - src/utils/diff-categorizer.js
  - src/core/config-manager.js
  - src/utils/efficient-prompt-builder.js
  - src/utils/optimized-diff-processor.js
requirements_addressed: []
autonomous: true
---

## Objective

Replace token estimation approximation with accurate tiktoken counting and implement diff size categorization using hybrid metrics (tokens, files, entities) to enable size-specific prompt strategies.

## Tasks

<task>
  <read_first>
    - package.json
  </read_first>
  <action>
    Install tiktoken package: npm install --save tiktoken
    Verify installation appears in dependencies section
  </action>
  <acceptance_criteria>
    - package.json dependencies contains "tiktoken"
    - npm list tiktoken shows installed version
    - require('tiktoken') does not throw error
  </acceptance_criteria>
</task>

<task>
  <read_first>
    - src/utils/efficient-prompt-builder.js
    - src/utils/optimized-diff-processor.js
  </read_first>
  <action>
    Create src/utils/token-counter.js module:
    - Import tiktoken: const { encoding_for_model } = require('tiktoken');
    - Create countTokens(text, model = 'gpt-4') function using encoding_for_model(model)
    - Handle null/empty input returning 0
    - Export countTokens function
  </action>
  <acceptance_criteria>
    - src/utils/token-counter.js exists
    - File exports countTokens function
    - countTokens uses encoding_for_model from tiktoken
    - grep -q "encoding_for_model" src/utils/token-counter.js
  </acceptance_criteria>
</task>

<task>
  <read_first>
    - src/utils/efficient-prompt-builder.js
  </read_first>
  <action>
    Replace all instances of Math.ceil(text.length / 4) with tokenCounter.countTokens(text):
    - Line 146 in compressPrompt: if (this.tokenCounter.countTokens(prompt) > this.maxPromptLength)
    - Update constructor to instantiate tokenCounter: this.tokenCounter = new TokenCounter()
    - Add require at top: const TokenCounter = require('./token-counter')
  </action>
  <acceptance_criteria>
    - src/utils/efficient-prompt-builder.js contains "require('./token-counter')"
    - src/utils/efficient-prompt-builder.js contains "this.tokenCounter = new TokenCounter()"
    - grep -q "Math.ceil(text.length / 4)" src/utils/efficient-prompt-builder.js returns no matches
  </acceptance_criteria>
</task>

<task>
  <read_first>
    - src/utils/optimized-diff-processor.js
  </read_first>
  <action>
    Replace estimateTokens method with accurate counting:
    - Add require: const TokenCounter = require('./token-counter')
    - In constructor: this.tokenCounter = new TokenCounter()
    - Replace estimateTokens method body to return this.tokenCounter.countTokens(text)
    - Keep method signature same for backward compatibility
  </action>
  <acceptance_criteria>
    - src/utils/optimized-diff-processor.js contains "require('./token-counter')"
    - src/utils/optimized-diff-processor.js contains "this.tokenCounter = new TokenCounter()"
    - estimateTokens method calls this.tokenCounter.countTokens
    - grep -q "Math.ceil(text.length / this.config.tokenEstimateFactor)" src/utils/optimized-diff-processor.js returns no matches
  </acceptance_criteria>
</task>

<task>
  <read_first>
    - .planning/phases/03-improve-the-relevance-of-the-git-commit-messages-for-small-and-large-diffs/03-CONTEXT.md
  </read_first>
  <action>
    Create src/utils/diff-categorizer.js module:
    - Import tokenCounter: const TokenCounter = require('./token-counter')
    - Implement categorizeDiff(diff, thresholds) function with defaults:
      * tokens: { small: 100, medium: 2000 } (large > 2000)
      * files: { small: 2, medium: 10 } (large > 10)
      * entities: { small: 5, medium: 20 } (large > 20)
    - Calculate metrics: tokenCount, fileCount, entityCount
    - Extract entities using regex: /function\s+(\w+)/, /const\s+(\w+)\s*=/, /class\s+(\w+)/, /var\s+(\w+)\s*=/, /let\s+(\w+)\s*=/
    - Return { category: 'small'|'medium'|'large', metrics: { tokens, files, entities } }
    - Use strict AND logic: all 3 metrics must agree, default to smallest
  </action>
  <acceptance_criteria>
    - src/utils/diff-categorizer.js exists
    - categorizeDiff function exported
    - categorizeDiff returns object with category and metrics properties
    - Entity extraction uses regex patterns for functions, classes, variables
    - grep -q "strict AND logic" src/utils/diff-categorizer.js
  </acceptance_criteria>
</task>

<task>
  <read_first>
    - src/core/config-manager.js
  </read_first>
  <action>
    Add user-configurable categorization thresholds:
    - Add DEFAULTS.categorization = { small: { tokens: 100, files: 2, entities: 5 }, medium: { tokens: 2000, files: 10, entities: 20 } }
    - Add get(key) method supporting 'categorization.small.tokens' dot notation
    - Add set(key, value) method for dot notation updates
    - Add validation to ensure numeric values for thresholds
  </action>
  <acceptance_criteria>
    - src/core/config-manager.js contains DEFAULTS.categorization object
    - get/set methods support dot notation for nested keys
    - grep -q "categorization" src/core/config-manager.js
  </acceptance_criteria>
</task>

<task>
  <read_first>
    - src/utils/efficient-prompt-builder.js
    - src/utils/diff-categorizer.js
  </read_first>
  <action>
    Integrate diff categorization into buildPrompt:
    - Add require: const DiffCategorizer = require('./diff-categorizer')
    - In constructor: this.diffCategorizer = new DiffCategorizer()
    - In buildPrompt method, after change analysis, add: const diffCategory = this.diffCategorizer.categorizeDiff(diff, options.categorizationThresholds)
    - Store category in options for later use: options.diffCategory = diffCategory
    - Log category for debugging: console.log(`Diff category: ${diffCategory.category}`)
  </action>
  <acceptance_criteria>
    - src/utils/efficient-prompt-builder.js contains "require('./diff-categorizer')"
    - buildPrompt method calls categorizeDiff
    - options.diffCategory is set with categorization result
    - grep -q "Diff category:" src/utils/efficient-prompt-builder.js
  </acceptance_criteria>
</task>

## Verification Criteria

1. Run `npm test` - all existing tests pass
2. Create test script: `node -e "const tc = require('./src/utils/token-counter'); console.log('Token count:', tc.countTokens('Hello world'))"` - outputs number without error
3. Test categorizer: `node -e "const dc = require('./src/utils/diff-categorizer'); console.log(dc.categorizeDiff('diff --git a/file.js b/file.js\n+ function test() {}'))"` - returns category 'small'
4. Test config: `aic config set categorization.small.tokens 150` - threshold updates successfully

## Must-Haves

- Use tiktoken library for accurate token counting (no approximation)
- Diff categorization uses 3 metrics: tokens, files, entities
- Fixed thresholds with strict AND logic (all must agree)
- Default to smallest category when metrics disagree
- User-configurable thresholds via config manager
- Categorization runs fresh on every call (no caching)
