---
wave: 2
depends_on:
  - 03-01-PLAN.md
files_modified:
  - package.json
  - src/utils/entity-extractor.js
  - src/utils/prompt-templates.js
  - src/utils/efficient-prompt-builder.js
  - tests/entity-extractor.test.js
requirements_addressed: []
autonomous: true
---

## Objective

Implement entity-centric prompts for small diffs with forced specificity instructions, explicit entity listing by type, and limited surrounding context to improve commit message precision.

## Tasks

<task>
  <read_first>
    - package.json
  </read_first>
  <action>
    Install handlebars package: npm install --save handlebars
    Verify installation appears in dependencies section
  </action>
  <acceptance_criteria>
    - package.json dependencies contains "handlebars"
    - npm list handlebars shows installed version
  </acceptance_criteria>
</task>

<task>
  <read_first>
    - .planning/phases/03-improve-the-relevance-of-the-git-commit-messages-for-small-and-large-diffs/03-CONTEXT.md
  </read_first>
  <action>
    Create src/utils/entity-extractor.js module:
    - Implement extractEntities(diff) function returning { functions: [], classes: [], variables: [], all: [] }
    - Use regex patterns for function detection: /function\s+(\w+)/g, /const\s+(\w+)\s*=\s*(async\s+)?(\(.*?\)|\w+)\s*=>/g, /(\w+)\s*:\s*function/g, /def\s+(\w+)/g, /public\s+(static\s+)?(\w+)/g
    - Use regex for class detection: /class\s+(\w+)/g, /interface\s+(\w+)/g, /type\s+(\w+)/g
    - Use regex for variable detection: /(?:let|const|var)\s+(\w+)\s*=/g, /this\.(\w+)\s*=/g, /(\w+)\s*:\s*[^{]/g
    - Remove duplicates and sort alphabetically
    - Filter out common false positives (if, for, while, return, etc.)
    - Return all entities flattened with type tags: [{ name: 'calculateTotal', type: 'function' }]
  </action>
  <acceptance_criteria>
    - src/utils/entity-extractor.js exists
    - extractEntities function exported
    - Returns object with functions, classes, variables arrays
    - Entity objects have name and type properties
    - grep -q "false positives" src/utils/entity-extractor.js
  </acceptance_criteria>
</task>

<task>
  <read_first>
    - src/utils/entity-extractor.js
    - tests/entity-extractor.test.js
  </read_first>
  <action>
    Create tests/entity-extractor.test.js:
    - Test extracting functions from diff: "diff --git a/file.js\n+ function calculateTotal() {}" expects functions: ['calculateTotal']
    - Test extracting classes: "diff --git a/file.js\n+ class UserManager {}" expects classes: ['UserManager']
    - Test extracting variables: "diff --git a/file.js\n+ const userCount = 0" expects variables: ['userCount']
    - Test removing duplicates: same entity mentioned twice appears only once
    - Test filtering false positives: 'if', 'for', 'while' not included as functions
    - Test empty diff returns empty arrays
    - Use Jest describe/test blocks
  </action>
  <acceptance_criteria>
    - tests/entity-extractor.test.js exists
    - All tests pass when running npm test
    - Test coverage for functions, classes, variables extraction
    - grep -q "describe('EntityExtractor'" tests/entity-extractor.test.js
  </acceptance_criteria>
</task>

<task>
  <read_first>
    - .planning/phases/03-improve-the-relevance-of-the-git-commit-messages-for-small-and-large-diffs/03-CONTEXT.md
  </read_first>
  <action>
    Create src/utils/prompt-templates.js with handlebars templates:
    - Import handlebars: const Handlebars = require('handlebars')
    - Create smallDiffTemplate with placeholders: {{category}}, {{entityList}}, {{entityCount}}, {{conventional}}, {{context}}
    - Template includes: "Small diff detected ({{category}}). Changed entities: {{entityList}}. Focus on WHAT specifically changed."
    - Create forcedSpecificityInstructions: "DO NOT use generic phrases like 'update file'. Be specific about WHAT changed."
    - Create entityListByType helper function to format: "Functions: calculateTotal, updateUser; Classes: UserManager"
    - Export compiled templates: buildSmallDiffPrompt, buildForcedSpecificityInstructions
  </action>
  <acceptance_criteria>
    - src/utils/prompt-templates.js exists
    - Uses Handlebars.compile for templates
    - smallDiffTemplate includes {{entityList}} placeholder
    - Template includes forced specificity instructions
    - grep -q "DO NOT use generic phrases" src/utils/prompt-templates.js
  </acceptance_criteria>
</task>

<task>
  <read_first>
    - src/utils/efficient-prompt-builder.js
    - src/utils/prompt-templates.js
  </read_first>
  <action>
    Integrate entity extraction and small diff templates into buildPrompt:
    - Add require: const EntityExtractor = require('./entity-extractor')
    - Add require: const PromptTemplates = require('./prompt-templates')
    - In constructor: this.entityExtractor = new EntityExtractor()
    - After diff categorization, if category === 'small':
      * Extract entities: const entities = this.entityExtractor.extractEntities(diff)
      * Build entity list by type
      * Add entity-centric section to prompt using PromptTemplates.buildSmallDiffPrompt
      * Include forced specificity instructions
    - Limit surrounding context to 2-3 lines before/after changes for small diffs
  </action>
  <acceptance_criteria>
    - src/utils/efficient-prompt-builder.js contains "require('./entity-extractor')"
    - src/utils/efficient-prompt-builder.js contains "require('./prompt-templates')"
    - buildPrompt checks options.diffCategory.category === 'small'
    - Entity list by type included in prompt for small diffs
    - grep -q "Changed entities:" src/utils/efficient-prompt-builder.js
  </acceptance_criteria>
</task>

<task>
  <read_first>
    - src/utils/efficient-prompt-builder.js
  </read_first>
  <action>
    Implement context line limiting for small diffs:
    - Create limitContextLines(diff, maxLines = 3) helper method
    - Parse diff into sections by file
    - For each changed line (+/-), include maxLines context before and after
    - Preserve diff headers (diff --git, index, ---, +++, @@)
    - Reassemble truncated diff
    - In buildPrompt, apply limiting when category === 'small'
    - Store result in options.truncatedDiff
  </action>
  <acceptance_criteria>
    - src/utils/efficient-prompt-builder.js contains limitContextLines method
    - Method accepts maxLines parameter defaulting to 3
    - Preserves diff headers and changed lines
    - Applied for small diffs in buildPrompt
    - grep -q "limitContextLines" src/utils/efficient-prompt-builder.js
  </acceptance_criteria>
</task>

<task>
  <read_first>
    - src/utils/efficient-prompt-builder.js
  </read_first>
  <action>
    Add single-line change highlighting for small diffs:
    - Create isSingleLineChange(diff) helper method
    - Check if diff has exactly one + or - line (excluding headers)
    - Create highlightSingleLine(diff) method that wraps the single change with ⬅️ marker
    - In buildPrompt for small diffs, if single-line detected:
      * Add "Single-line change detected:" to prompt
      * Include highlighted line in prompt
      * Add "Reflect this exact modification" instruction
  </action>
  <acceptance_criteria>
    - src/utils/efficient-prompt-builder.js contains isSingleLineChange method
    - src/utils/efficient-prompt-builder.js contains highlightSingleLine method
    - Single-line changes get special handling in prompt
    - grep -q "Single-line change detected" src/utils/efficient-prompt-builder.js
  </acceptance_criteria>
</task>

## Verification Criteria

1. Run `npm test` - all tests pass including new entity-extractor tests
2. Test small diff prompt: create diff with single function change, verify entity appears in generated prompt
3. Test entity extraction: `node -e "const ee = require('./src/utils/entity-extractor'); const diff = '+ function calculateTotal() {}'; console.log(ee.extractEntities(diff))"` - returns { functions: ['calculateTotal'], classes: [], variables: [] }
4. Verify context limiting: small diff with 10 context lines gets truncated to 3 lines in prompt
5. Test single-line highlighting: diff with one + line includes ⬅️ marker in prompt

## Must-Haves

- Entity extraction uses regex patterns for functions, classes, variables
- Small diff prompts list all entities explicitly by type
- Forced specificity instructions included: "DO NOT use generic phrases"
- Surrounding context limited to 2-3 lines for small diffs
- Single-line changes get special highlighting
- Use handlebars for prompt templates (no string concatenation)
