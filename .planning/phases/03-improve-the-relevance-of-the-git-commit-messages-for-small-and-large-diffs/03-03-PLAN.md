---
wave: 3
depends_on:
  - 03-01-PLAN.md
  - 03-02-PLAN.md
files_modified:
  - package.json
  - src/utils/diff-summarizer.js
  - src/utils/prompt-templates.js
  - src/utils/efficient-prompt-builder.js
  - tests/diff-summarizer.test.js
requirements_addressed: []
autonomous: true
---

## Objective

Implement hierarchical summarization for large diffs by reusing existing chunking logic, extracting key changes per chunk, and combining summaries into a coherent commit message.

## Tasks

<task>
  <read_first>
    - package.json
  </read_first>
  <action>
    Install parse-diff package: npm install --save parse-diff
    Verify installation appears in dependencies section
  </action>
  <acceptance_criteria>
    - package.json dependencies contains "parse-diff"
    - npm list parse-diff shows installed version
  </acceptance_criteria>
</task>

<task>
  <read_first>
    - .planning/phases/03-improve-the-relevance-of-the-git-commit-messages-for-small-and-large-diffs/03-CONTEXT.md
    - src/utils/optimized-diff-processor.js
  </read_first>
  <action>
    Create src/utils/diff-summarizer.js module:
    - Import parse-diff: const parseDiff = require('parse-diff')
    - Import tokenCounter: const TokenCounter = require('./token-counter')
    - Implement extractFileChunks(diff) using parse-diff to get structured file data
    - Implement summarizeChunk(chunk, index, total) returning summary object with { file, changes, entities }
    - Use existing chunking from OptimizedDiffProcessor for large diffs
    - Store chunk summaries in array for later combination
    - Export: extractFileChunks, summarizeChunk, combineSummaries
  </action>
  <acceptance_criteria>
    - src/utils/diff-summarizer.js exists
    - Uses parse-diff library for diff parsing
    - extractFileChunks function exported
    - summarizeChunk function exported
    - grep -q "parse-diff" src/utils/diff-summarizer.js
  </acceptance_criteria>
</task>

<task>
  <read_first>
    - src/utils/entity-extractor.js
    - src/utils/diff-summarizer.js
  </read_first>
  <action>
    Enhance summarizeChunk to extract key changes per chunk:
    - Import EntityExtractor: const EntityExtractor = require('./entity-extractor')
    - In summarizeChunk, extract entities from chunk content
    - Identify change type by analyzing chunk (feat, fix, refactor, etc.)
    - Return { fileName, changeType, entities: { functions, classes, variables }, keyChanges: string[] }
    - Limit keyChanges to top 3 most significant changes per chunk
    - Use existing analyzeDiffForSpecialization logic for change type inference
  </action>
  <acceptance_criteria>
    - summarizeChunk returns object with fileName, changeType, entities properties
    - Key changes limited to top 3 per chunk
    - Uses EntityExtractor for entity detection
    - grep -q "keyChanges" src/utils/diff-summarizer.js
  </acceptance_criteria>
</task>

<task>
  <read_first>
    - src/utils/prompt-templates.js
  </read_first>
  <action>
    Add large diff prompt templates to prompt-templates.js:
    - Create largeDiffTemplate with placeholders: {{chunkCount}}, {{chunkSummaries}}, {{conventional}}
    - Create chunkSummaryPrompt: "Summarize this chunk into 1-2 key changes. Focus on WHAT changed, not implementation details."
    - Create combineSummariesPrompt: "Combine these {{chunkCount}} summaries into one cohesive commit message."
    - Add registerHelpers function to register custom Handlebars helpers if needed
    - Export: buildLargeDiffPrompt, buildChunkSummaryPrompt, buildCombineSummariesPrompt
  </action>
  <acceptance_criteria>
    - src/utils/prompt-templates.js contains largeDiffTemplate
    - Template uses Handlebars with {{chunkCount}} placeholder
    - chunkSummaryPrompt includes "1-2 key changes" instruction
    - grep -q "Combine these" src/utils/prompt-templates.js
  </acceptance_criteria>
</task>

<task>
  <read_first>
    - tests/diff-summarizer.test.js
  </read_first>
  <action>
    Create tests/diff-summarizer.test.js:
    - Test extractFileChunks with multi-file diff
    - Test summarizeChunk returns correct structure with fileName, changeType, entities
    - Test entity extraction from chunk content
    - Test keyChanges limited to 3 items
    - Test empty chunk returns empty arrays
    - Test combineSummaries merges multiple chunk summaries
    - Use Jest describe/test blocks with beforeEach for isolation
  </action>
  <acceptance_criteria>
    - tests/diff-summarizer.test.js exists
    - All tests pass when running npm test
    - Tests cover extractFileChunks, summarizeChunk, combineSummaries
    - grep -q "describe('DiffSummarizer'" tests/diff-summarizer.test.js
  </acceptance_criteria>
</task>

<task>
  <read_first>
    - src/utils/efficient-prompt-builder.js
    - src/utils/diff-summarizer.js
    - src/utils/prompt-templates.js
  </read_first>
  <action>
    Integrate hierarchical summarization into buildPrompt for large diffs:
    - Add require: const DiffSummarizer = require('./diff-summarizer')
    - In constructor: this.diffSummarizer = new DiffSummarizer()
    - After diff categorization, if category === 'large':
      * Extract file chunks: const chunks = this.diffSummarizer.extractFileChunks(diff)
      * Generate chunk summaries array
      * Build prompt using PromptTemplates.buildLargeDiffPrompt with chunkCount and summaries
      * Add "Processing {N} chunks in parallel..." message
      * Store summaries in options.chunkSummaries for later combination
    - For each chunk, create individual prompt using buildChunkSummaryPrompt
  </action>
  <acceptance_criteria>
    - src/utils/efficient-prompt-builder.js contains "require('./diff-summarizer')"
    - buildPrompt checks options.diffCategory.category === 'large'
    - Calls extractFileChunks and builds summaries for large diffs
    - Prompt includes "Processing N chunks" message
    - grep -q "chunks in parallel" src/utils/efficient-prompt-builder.js
  </acceptance_criteria>
</task>

<task>
  <read_first>
    - src/utils/efficient-prompt-builder.js
    - src/utils/prompt-templates.js
  </read_first>
  <action>
    Implement chunk combination logic for large diffs:
    - Create combineChunkSummaries(summaries, conventional) method
    - Use PromptTemplates.buildCombineSummariesPrompt with summaries array
    - Add instruction: "Combine these into one commit message following conventional format"
    - Ensure combined message preserves key changes from all chunks
    - Return combined prompt for final AI generation
    - Call this method after receiving all chunk summaries
  </action>
  <acceptance_criteria>
    - src/utils/efficient-prompt-builder.js contains combineChunkSummaries method
    - Method uses buildCombineSummariesPrompt from templates
    - Instruction includes "one commit message"
    - grep -q "combineChunkSummaries" src/utils/efficient-prompt-builder.js
  </acceptance_criteria>
</task>

<task>
  <read_first>
    - src/utils/efficient-prompt-builder.js
  </read_first>
  <action>
    Reuse existing chunking logic for hierarchical processing:
    - In buildPrompt for large diffs, call OptimizedDiffProcessor.processDiffWithStrategy
    - Use existing adaptive-chunking or balanced-chunking strategies
    - Reuse maxPromptLength limit of 8000 tokens for chunks
    - Preserve existing chunk boundaries (file boundaries, semantic boundaries)
    - Add summarization layer on top of existing chunks
    - Log strategy used: console.log(`Chunking strategy: ${analysis.processingStrategy}`)
  </action>
  <acceptance_criteria>
    - Uses OptimizedDiffProcessor for chunking large diffs
    - Preserves existing maxPromptLength of 8000
    - Reuses file and semantic boundary detection
    - Logs chunking strategy
    - grep -q "Chunking strategy:" src/utils/efficient-prompt-builder.js
  </acceptance_criteria>
</task>

## Verification Criteria

1. Run `npm test` - all tests pass including new diff-summarizer tests
2. Test large diff with 3 files: verify 3 chunk summaries generated
3. Test chunk extraction: `node -e "const ds = require('./src/utils/diff-summarizer'); const diff = 'diff --git a/file1.js b/file1.js\n+ const x = 1'; console.log(ds.extractFileChunks(diff))"` - returns array of file chunks
4. Test summarization: large diff produces "Processing N chunks" message in prompt
5. Test combination: 3 chunk summaries combine into single "Combine these" prompt
6. Verify existing chunking not broken: 50KB diff still chunks correctly at 8000 tokens

## Must-Haves

- Use parse-diff for diff parsing (NOT regex)
- Reuse existing chunking logic from OptimizedDiffProcessor
- Extract key changes per chunk (top 3 most significant)
- Combine all summaries into final commit message
- Hierarchical approach: chunk → summarize → combine
- Prompt instruction: "Combine these into one commit message"
