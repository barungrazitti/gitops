---
phase: 01-stability-error-recovery
plan: 01
type: execute
wave: 1
depends_on: []
files_modified: [tests/error-handling.test.js, src/index.js]
autonomous: true
requirements: [STAB-01, STAB-03, STAB-04, QUAL-04]

must_haves:
  truths:
    - "AICommitGenerator.provideErrorSuggestions exists as a method"
    - "provideErrorSuggestions returns local suggestions for common Git errors"
    - "Calling generate() with an error-inducing state does not crash the process"
  artifacts:
    - path: "tests/error-handling.test.js"
      provides: "Unit tests for error identification and local suggestions"
    - path: "src/index.js"
      provides: "provideErrorSuggestions implementation"
  key_links:
    - from: "src/index.js (generate method)"
      to: "this.provideErrorSuggestions"
      via: "catch block call"
      pattern: "this\\.provideErrorSuggestions\\(error, .*\\)"
---

<objective>
Implement the foundation for error recovery by adding the missing `provideErrorSuggestions` method and local fallback logic. This prevents the reported crash and provides immediate value with local troubleshooting tips.

Purpose: Fix critical QUAL-04 crash and satisfy STAB-01/STAB-03 requirements.
Output: Working error suggestion system with local knowledge base.
</objective>

<execution_context>
@/Users/barun.tayenjam/.gemini/get-shit-done/workflows/execute-plan.md
@/Users/barun.tayenjam/.gemini/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@01-RESEARCH.md
@src/index.js
</context>

<tasks>

<task type="auto">
  <name>Task 0: Create test scaffold</name>
  <files>tests/error-handling.test.js</files>
  <action>
    Create a new Jest test file for error handling. 
    Define tests for:
    1. `identifyErrorType`: Should categorize errors based on messages (e.g., "no staged changes", "not a git repository", "401", "429").
    2. `getLocalSuggestion`: Should return correct actionable strings for identified types.
    3. `provideErrorSuggestions`: Should handle being called without crashing and print to console.
  </action>
  <verify>
    <automated>touch tests/error-handling.test.js && npm test tests/error-handling.test.js</automated>
  </verify>
  <done>Test file exists and initial failing tests for missing methods are created.</done>
</task>

<task type="auto">
  <name>Task 1: Implement Error Identification & Local Suggestions</name>
  <files>src/index.js</files>
  <action>
    Implement internal helper methods in `AICommitGenerator` class:
    1. `identifyErrorType(error)`: Returns a string slug (e.g., 'git_no_changes', 'ai_auth_error') based on `error.message`. Use the taxonomy from 01-RESEARCH.md.
    2. `getLocalSuggestion(type)`: Returns a user-friendly string based on the type.
    
    Add the `provideErrorSuggestions(error, options)` method:
    - Call `identifyErrorType`.
    - Get `getLocalSuggestion`.
    - Use `chalk` to print the suggestion to the console in yellow.
    - Wrap the whole method in a try-catch that fails silently (pitfall avoidance).
  </action>
  <verify>
    <automated>npm test tests/error-handling.test.js</automated>
  </verify>
  <done>Methods are implemented and tests pass for local suggestion logic.</done>
</task>

<task type="auto">
  <name>Task 2: Wire into main execution loop</name>
  <files>src/index.js</files>
  <action>
    Update the `generate` method's `catch` block (around L205) to ensure `provideErrorSuggestions` is called safely before re-throwing the error.
    
    Also, add `provideErrorSuggestions` to any other relevant error paths discovered in `src/index.js` (e.g., in interactive selection).
  </action>
  <verify>
    <automated>npm test tests/error-handling.test.js</automated>
  </verify>
  <done>The application no longer crashes when an error occurs; instead, it shows a helpful suggestion.</done>
</task>

</tasks>

<verification>
Run the new test suite and manually verify by running aic in a non-git directory or with no changes staged.
</verification>

<success_criteria>
- tests/error-handling.test.js passes.
- AICommitGenerator has provideErrorSuggestions, identifyErrorType, and getLocalSuggestion.
- No crash on error in generate().
</success_criteria>

<output>
After completion, create `.planning/phases/01-stability-error-recovery/01-01-SUMMARY.md`
</output>
