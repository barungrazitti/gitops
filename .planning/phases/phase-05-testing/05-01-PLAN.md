---
phase: 05-testing-polish
plan: 01
type: execute
wave: 1
depends_on:
  - 04-01-PLAN.md
  - 04-02-PLAN.md
files_modified:
  - tests/integration/pipeline.test.js
  - tests/integration/auto-workflow.test.js
  - tests/integration/quality-gates.test.js
  - src/core/cache-manager.js
  - src/utils/diff-analyzer.js
autonomous: false
requirements:
  - QUAL-03
  - QUAL-04
  - CROSS-01
  - CROSS-02
must_haves:
  truths:
    - "Integration tests cover full pipeline: diff → detect → generate → validate → commit"
    - "Performance targets: <10s generation, <1s cache hit, <500ms startup"
    - "Cache effectiveness: >95% hit rate for similar changes"
    - "Activity logging captures all AI interactions and git operations"
    - "Usage statistics track commits, providers, success rates"
  artifacts:
    - path: tests/integration/pipeline.test.js
      provides: "End-to-end pipeline tests"
      min_lines: 150
    - path: tests/integration/auto-workflow.test.js
      provides: "Auto-workflow integration tests"
      min_lines: 120
    - path: tests/integration/quality-gates.test.js
      provides: "Quality threshold validation tests"
      min_lines: 100
    - path: src/core/cache-manager.js
      provides: "Optimized caching with semantic similarity"
      min_lines: 200
  key_links:
    - from: tests/integration/pipeline.test.js
      to: src/core/commit-generator.js
      via: "Tests full generation pipeline"
      pattern: "require.*commit-generator"
    - from: src/core/cache-manager.js
      to: src/utils/diff-analyzer.js
      via: "Uses semantic analysis for cache keys"
      pattern: "require.*diff-analyzer"
---

<objective>
Build comprehensive integration tests and performance optimizations — the final polish that ensures quality, performance, and reliability.

Integration tests verify the full pipeline: git diff → detector analysis → AI generation → quality validation → commit. Tests run against real git repositories with actual AI providers (mocked in CI).

Performance optimizations focus on hot paths: cache hit rate improvement, diff processing speed, and AI response time. Target: <10s for typical diffs, <1s cache hits, >95% cache effectiveness.

Purpose: Production-ready quality with verified performance targets.
Output: Integration test suites and optimized cache/diff handling.
</objective>

<execution_context>
@$HOME/.config/opencode/get-shit-done/workflows/execute-plan.md
@$HOME/.config/opencode/get-shit-done/templates/summary.md
</execution_context>

<context>
@.planning/PROJECT.md
@.planning/ROADMAP.md
@.planning/STATE.md
@.planning/REQUIREMENTS.md

<interfaces>
<!-- Existing code contracts the executor needs -->

From src/core/cache-manager.js — existing caching:
```javascript
class CacheManager {
  async getValidated(diff) // returns cached messages or null
  async setValidated(diff, messages) // caches messages
  // Uses semantic similarity for cache keys
}
```

From src/utils/diff-analyzer.js — semantic analysis:
```javascript
class DiffAnalyzer {
  analyzeDiffContent(diff) // returns semantic insights
  // Detects functions, classes, components, API changes
}
```

From src/core/activity-logger.js — logging:
```javascript
class ActivityLogger {
  logAIInteraction(provider, type, prompt, response, duration, success)
  logGitOperation(operation, details)
  info(event, data) // generic info logging
}
```

From src/core/stats-manager.js — statistics:
```javascript
class StatsManager {
  recordCommit(provider) // increments commit count
  getUsageStats(days) // returns stats for period
}
```
</interfaces>
</context>

<tasks>

<task type="auto" tdd="false">
  <name>Task 1: Integration Tests — Full Pipeline (QUAL-03)</name>
  <files>tests/integration/pipeline.test.js</files>
  <behavior>
    - Given real git repo with staged changes, pipeline generates valid commit message
    - Given component changes, message includes component scope
    - Given dependency changes, message mentions affected files
    - Given AI provider available, generation completes <10s
    - Given cache hit, response returns <1s
    - Given quality validation, generic messages rejected
    - Given dry-run mode, no commit created but messages returned
  </behavior>
  <action>
    Create `tests/integration/pipeline.test.js` — Full pipeline integration tests:

    **Test setup:**
    - Create temporary git repository in /tmp
    - Initialize with sample files (component structure)
    - Configure mock AI provider (or skip if CI without credentials)
    - Clean up after each test

    **Test cases:**

    1. **Happy path:** Stage changes → generate → validate → commit
       - Create src/utils/helper.js with new function
       - Stage file
       - Run CommitGenerator.generate()
       - Assert: Message generated with conventional format
       - Assert: Message includes file path "helper.js"
       - Assert: Message includes reasoning (why section)
       - Assert: Commit created in git log

    2. **Component detection:** Change component file → scope in message
       - Modify src/auth/login.js
       - Assert: Message includes "auth" scope
       - Assert: Message mentions "login" component

    3. **Dependency impact:** Change shared module → affected files noted
       - Modify src/core/base.js (imported by 3 files)
       - Assert: Message mentions "affects 3 files" or similar
       - Assert: Dependency context included in prompt

    4. **Cache hit performance:** Same diff twice → second is instant
       - Generate for diff A (cold)
       - Generate for diff A again (warm)
       - Assert: Second call <1s
       - Assert: Messages identical

    5. **Quality gate:** Generic message → rejected
       - Mock AI to return "update code"
       - Assert: Validator rejects message
       - Assert: Error includes suggestions

    6. **Dry run:** --dry-run flag → no commit created
       - Stage changes
       - Generate with dryRun: true
       - Assert: Messages returned
       - Assert: Git log unchanged

    Use `require('simple-git')` for git operations.
    Use `require('tmp')` for temp directories.
    Jest `describe()` and `it()` blocks.
    Skip tests in CI without AI credentials.
  </action>
  <verify>
    <automated>npx jest tests/integration/pipeline.test.js --verbose --testTimeout=30000</automated>
  </verify>
  <done>Integration tests cover full pipeline with real git repo. All 6 test cases pass. Performance targets verified.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 2: Integration Tests — Auto-Workflow</name>
  <files>tests/integration/auto-workflow.test.js</files>
  <behavior>
    - Given clean repo with changes, auto() completes full workflow
    - Given merge conflicts, AI resolution merges intelligently
    - Given --skip-pull, pull step skipped
    - Given --no-push, push step skipped
    - Given error mid-workflow, rollback cleans up partial state
  </behavior>
  <action>
    Create `tests/integration/auto-workflow.test.js` — Auto-workflow integration tests:

    **Test setup:**
    - Create temporary git repository
    - Set up remote (mock with local bare repo)
    - Create sample conflicts for conflict tests
    - Clean up after each test

    **Test cases:**

    1. **Full auto workflow:** Changes → auto() → committed and pushed
       - Create and stage changes
       - Run AutoWorkflow.auto({ skipPull: true, noPush: false })
       - Assert: Commit created
       - Assert: Push successful (to mock remote)

    2. **Conflict resolution:** Conflicting changes → AI resolves
       - Create conflict in test file
       - Run AutoWorkflow.auto({ conflictStrategy: 'ai' })
       - Assert: Conflicts resolved
       - Assert: Merged file has both changes intelligently combined
       - Assert: Commit created after resolution

    3. **Skip pull:** --skip-pull flag → no pull attempted
       - Run AutoWorkflow.auto({ skipPull: true })
       - Assert: Pull not called
       - Assert: Commit and push still executed

    4. **No push:** --no-push flag → push skipped
       - Run AutoWorkflow.auto({ noPush: true })
       - Assert: Push not called
       - Assert: Commit created

    5. **Rollback on failure:** Error mid-workflow → state cleaned
       - Mock push to fail
       - Run AutoWorkflow.auto()
       - Assert: Commit rolled back (git reset)
       - Assert: Files unstaged
       - Assert: Error logged

    Use `require('fs-extra')` for file operations.
    Use `require('simple-git')` for git operations.
    Mock AI provider for conflict resolution.
  </action>
  <verify>
    <automated>npx jest tests/integration/auto-workflow.test.js --verbose --testTimeout=30000</automated>
  </verify>
  <done>Auto-workflow integration tests pass. Conflict resolution, skip options, and rollback all verified.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 3: Quality Gates Validation (QUAL-01, QUAL-02)</name>
  <files>tests/integration/quality-gates.test.js</files>
  <behavior>
    - Given 100 messages, <5 are generic (QUAL-01)
    - Given 100 messages, >90 include reasoning (QUAL-02)
    - Given batch validation, stats accurately reported
    - Given threshold failure, helpful error provided
  </behavior>
  <action>
    Create `tests/integration/quality-gates.test.js` — Quality threshold validation:

    **Test setup:**
    - Sample messages from real AI generations (or mocked)
    - Mix of valid and invalid messages for threshold testing

    **Test cases:**

    1. **QUAL-01: Generic rate <5%**
       - Generate 100 messages (mocked with known distribution)
       - Include 4 generic messages ("update code", "fix bug", etc.)
       - Run MessageValidator.validateBatch()
       - Assert: genericCount === 4
       - Assert: genericRate === 0.04 (< 0.05)
       - Assert: QUAL-01 passes

    2. **QUAL-01 violation: Generic rate >=5%**
       - Include 6 generic messages out of 100
       - Run validation
       - Assert: QUAL-01 fails
       - Assert: Error includes "Too many generic messages"

    3. **QUAL-02: Reasoning rate >90%**
       - Generate 100 messages with reasoning
       - Include 90 with "to fix", "enables", "because", etc.
       - Run validation
       - Assert: withReasoning === 90
       - Assert: reasoningRate === 0.90 (>= 0.90)
       - Assert: QUAL-02 passes

    4. **QUAL-02 violation: Reasoning rate <90%**
       - Include only 80 with reasoning out of 100
       - Run validation
       - Assert: QUAL-02 fails
       - Assert: Error includes "Insufficient reasoning"

    5. **Batch validation stats accuracy**
       - Validate batch of 20 messages
       - Mix: 15 valid, 3 generic, 2 no-reasoning
       - Assert: stats.total === 20
       - Assert: stats.validCount === 15
       - Assert: stats.genericCount === 3
       - Assert: stats.withReasoning === 17
       - Assert: stats.qualityRate === 0.75

    6. **Suggestions generation**
       - Given generic message, get suggestions
       - Assert: Suggestions include "Be more specific"
       - Assert: Suggestions mention function/class names

    Use Jest `describe()` and `it()` blocks.
    Mock AI responses for controlled testing.
  </action>
  <verify>
    <automated>npx jest tests/integration/quality-gates.test.js --verbose</automated>
  </verify>
  <done>Quality gates tests pass. QUAL-01 and QUAL-02 thresholds validated. Stats accuracy verified.</done>
</task>

<task type="auto" tdd="false">
  <name>Task 4: Cache Optimization (QUAL-04)</name>
  <files>src/core/cache-manager.js</files>
  <behavior>
    - Given similar diff, cache hit rate >95%
    - Given semantic similarity, cache key matches related changes
    - Given cache full, LRU eviction works correctly
    - Given stale cache, TTL expiration works
  </action>
  <action>
    Optimize `src/core/cache-manager.js` — Enhanced caching with semantic similarity:

    **Current implementation review:**
    - Check existing cache key generation
    - Identify opportunities for semantic similarity

    **Optimizations:**

    1. **Semantic cache keys:**
       - Use DiffAnalyzer to extract semantic signature
       - Key = hash(functionNames + classNames + filePaths)
       - Similar changes → similar keys → higher hit rate

    2. **LRU eviction:**
       - Track access times for cache entries
       - When max size reached, evict least recently used
       - Max size: 1000 entries (configurable)

    3. **TTL management:**
       - Default TTL: 7 days
       - Configurable via options
       - Expired entries cleaned on access

    4. **Cache statistics:**
       - Track hits, misses, evictions
       - Expose getStats() for monitoring
       - Log hit rate for debugging

    5. **Diff similarity check:**
       - Before cache miss, check Jaccard similarity
       - If similarity > 0.85, return cached result with note
       - Increases effective hit rate

    **Methods to implement/update:**
    - `generateCacheKey(diff)` — Use semantic signature
    - `calculateSimilarity(diff1, diff2)` — Jaccard similarity
    - `getStats()` — Return cache statistics
    - `evictLRU()` — Evict least recently used
    - `cleanupExpired()` — Remove expired entries

    Use `require('node-cache')` for base caching.
    Use `crypto` for hashing.
    CommonJS `module.exports = CacheManager`.

    Per QUAL-04: >95% cache hit rate for similar changes.
  </action>
  <verify>
    <automated>npx jest tests/core/cache-manager.test.js --verbose</automated>
  </verify>
  <done>Cache manager optimized with semantic keys, LRU eviction, and similarity matching. Hit rate >95% for similar changes.</done>
</task>

</tasks>

<verification>
1. Run all integration tests: `npx jest tests/integration/ --verbose --testTimeout=30000`
2. Check cache hit rate: Run generation twice on same diff, verify second is <1s
3. Verify quality thresholds: `rg 'QUAL-01|QUAL-02' tests/integration/quality-gates.test.js`
4. Test auto-workflow: Create test repo with conflicts, run `aic --conflict-strategy=ai`
5. Full test suite: `npm test` — all tests pass (including pre-existing 14 failures documented)
</verification>

<success_criteria>
- Integration tests cover full pipeline (6 test cases)
- Auto-workflow tests cover conflicts and rollback (5 test cases)
- Quality gates tests validate QUAL-01 and QUAL-02 (6 test cases)
- Cache hit rate >95% for similar changes
- Performance targets met: <10s generation, <1s cache hit
- All pre-existing test failures documented (not regressions from this work)
- Activity logging captures all interactions
</success_criteria>

<output>
After completion, create `.planning/phases/phase-05-testing/05-01-SUMMARY.md`
</output>
