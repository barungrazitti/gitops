---
wave: 2
depends_on:
  - 02-01-PLAN.md
  - 02-02-PLAN.md
files_modified:
  - src/**/*.js
  - tests/**/*.js
  - bin/**/*.js
  - scripts/**/*.js
requirements_addressed:
  - QUAL-03
autonomous: false
---

## Objective

Run ESLint on the src/ directory and fix all reported errors until the linter passes with 0 errors, ensuring the codebase meets Airbnb style guide standards.

## Tasks

<task>
  <read_first>
    - .eslintrc.json
    - package.json
  </read_first>
  <action>
    Run ESLint on the src/ directory to identify all linting issues:
    
    npm run lint 2>&1 | tee .planning/phases/02-quality-standards/eslint-report.txt
    
    This will execute ESLint and save the full report to a file for analysis.
  </action>
  <acceptance_criteria>
    - Command completes and produces output
    - File .planning/phases/02-quality-standards/eslint-report.txt exists with ESLint results
  </acceptance_criteria>
</task>

<task>
  <read_first>
    - .planning/phases/02-quality-standards/eslint-report.txt
  </read_first>
  <action>
    Analyze the ESLint report to categorize errors by type:
    
    1. Undeclared variables (no-undef errors)
    2. Unused imports/variables (no-unused-vars errors)
    3. Console.log statements (no-console warnings)
    4. Import/export issues (import/* errors)
    5. Code style violations (indentation, quotes, spacing)
    6. Other Airbnb style guide violations
    
    Create a summary of error counts by category and file locations.
  </action>
  <acceptance_criteria>
    - Error analysis completed with counts by category
    - List of files requiring fixes identified
  </acceptance_criteria>
</task>

<task>
  <read_first>
    - src/**/*.js
  </read_first>
  <action>
    Attempt automatic fixes using ESLint's --fix flag:
    
    npm run lint:fix
    
    This will automatically fix all auto-correctable issues (spacing, quotes, semicolons, trailing commas, etc.).
  </action>
  <acceptance_criteria>
    - Command completes successfully
    - Number of remaining (non-auto-fixable) errors identified
  </acceptance_criteria>
</task>

<task>
  <read_first>
    - src/**/*.js
    - .eslintrc.json
  </read_first>
  <action>
    Manually fix remaining ESLint errors that cannot be auto-corrected:
    
    1. **Undeclared variables (no-undef)**: Add proper require() statements or declare missing variables
    2. **Unused imports/variables**: Remove unused imports or prefix variable names with underscore (_) if intentionally unused
    3. **Console.log statements**: Remove console.log statements or replace with proper logging utilities
    4. **Import/export issues**: Fix import paths, add missing extensions (.js), or resolve circular dependencies
    5. **Code style violations**: Manually adjust code to comply with Airbnb style guide
    
    Process files one at a time, running `npm run lint` after each fix to verify progress.
  </action>
  <acceptance_criteria>
    - ESLint report shows 0 errors for src/ directory
    - All files in src/ pass ESLint validation
    - Code functionality remains unchanged (no bugs introduced)
  </acceptance_criteria>
</task>

<task>
  <read_first>
    - package.json
    - .eslintrc.json
  </read_first>
  <action>
    Run final verification to ensure all linting issues in src/ are resolved:
    
    npm run lint
    
    Expected output: ESLint runs successfully with 0 errors and 0 warnings for src/ directory.
  </action>
  <acceptance_criteria>
    - Command `npm run lint` completes with exit code 0
    - No errors reported for files in src/ directory
    - ESLint output shows clean run for src/
  </acceptance_criteria>
</task>

## Verification Criteria

1. Running `npm run lint` produces 0 errors for the src/ directory
2. All ESLint errors are categorized and fixed appropriately
3. Code remains functional with no breaking changes
4. ESLint report saved for documentation purposes

## Must-Haves

- All ESLint errors in src/ directory must be fixed (0 errors required)
- No rule overrides or exceptions added to .eslintrc.json
- Undeclared variables must be properly declared
- Unused imports/variables must be removed or marked with underscore prefix
- Console.log statements must be removed (only in production code, not test setup)
- Code functionality must be preserved (no behavior changes)
