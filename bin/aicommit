#!/bin/bash
PKG_LINK="$(cd "$(dirname "$0")/../lib/node_modules/ai-commit-generator" && pwd -P 2>/dev/null)" || {
  echo "aicommit: package not found" >&2
  exit 1
}
cd "$PKG_LINK" || exit 1
exec node bin/aicommit.js "$@"
